Follow¼¯ºÏ£º
Start: #
PROG: #
DECLS: if read id write { while bool int
DECL: if read id write { while bool int
NAMES: , ;
NAME: , ;
STMTS: if read id write { while }
STMT: if read id write { while } else
EXPR: - != > >= == < ) && || ; <= +
ADD: - id number (
TERM: <= - != > >= * == < ) && || ; / +
MUL: - id number (
NEGA: <= - != > >= * == < ) && || ; / +
FACTOR: <= - != > >= * == < ) && || ; / +
BOOL: || ;
JOIN: || && ;
NOT: ; || &&
REL: || && ;
ROP: - id number (
