#include <bits/stdc++.h>
set<int> s;
int count()
{
    char flag, var, start, end;
    cin >> flag ;
    if (flag== 'F'){
        cin >> var >> start >> end;
        
    }

}
using namespace std;
int main()
{
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        int lines;
        string s;
        cin >> lines >> s;
        if (lines >= 2)
        {
            count();
        }
    }
}