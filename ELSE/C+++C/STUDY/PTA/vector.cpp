#include <bits/stdc++.h>
using namespace std;
#define MAXN 100000
int a[MAXN];
int n, d, k;
vector<int> datas[MAXN];
int main()
{
    ios::sync_with_stdio(false);
    cin >> n >> d >> k;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < d; j++)
        {
            int temp;
            cin >> temp;
            datas[i].push_back(temp);
        }
        // for ()
    }
}