#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
ll N;
ll C(int a, int b)//??i??j???
{
	ll res = 1;
	for (ll i = a, j = 1; j <= b; i--, j++)
	{
		res = res * i / j;
		if (res > N)//????????N?????
			return res;
	}
	return res;
}
int main()
{
	cin >> N;
	for (int k = 16; k >= 0; k--)//??????
	{
		ll l = 2 * k, r = max(N, l), mid;
		while (l <= r) {//??k??????
			mid = l + (r - l) / 2;//???
			ll CC = C(mid, k);
			if (CC == N)
				break;
			else if (CC > N)
				r = mid - 1;
			else
				l = mid + 1;
		}
		if (C(mid, k) == N)
		{//?mid???k?????N
			cout << (mid + 1) * mid / 2 + k + 1 << endl;
			//?????????????1?????,??????????
			//????????1(????0??)?????????
			break;
		}
	}
	return 0;
}
